[project]
name = "ovd-mcp-nats"
version = "0.1.0"
description = "OVD Platform — MCP Server NATS JetStream"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
    "nats-py>=2.9.0",
]

[tool.uv]
